# Art Assets

This directory is intentionally sparse during the prototype.

Do not commit generated concept/reference images here as production assets.

Production-ready commissioned, licensed, or intentionally sourced artwork should be exported into the paths defined in `data/art_manifest.json`.

Until a slot has a real asset, scenes should use the game's fallback UI/placeholders.
